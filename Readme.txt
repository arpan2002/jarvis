You need to import and install all of these code to run a programe.

import pyttsx3 #pip install pyttsx3
import speech_recognition as sr #pip install speechRecognition
import datetime
import wikipedia #pip install wikipedia
import webbrowser
import os
import smtplib

#There are two voices, Voice(1) For Women and Voice(2) for Men